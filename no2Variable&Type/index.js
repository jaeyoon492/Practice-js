let a = 221; //let은 변수를 지정할때 쓰는 명령어 이다.

let b = a - 5;

a = 4; //a를 4로 초기화 했기에 이제부터 변수 a는 4이다.

console.log(a);
//java 처럼 변수의 타입을 각각 지정하지 않아도 되어 편하다.

const c = 111; //const 는 상수를 지정할때 쓰는 명령어 이다.

var d = 222; //var는 Variable(변수)를 뜻한다. 3년전엔 var 뿐이 없었다.

const str = "jevari"; //String 저장에도 const를 쓸수 있다.

const tr = true; //Boolean 형태

const fl = 55.1; //Float 형태 실수형(소수) 숫자 사용시
